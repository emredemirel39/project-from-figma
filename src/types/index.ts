export interface IAccordionItem {
    id: number,
    title: string,
    text: string
};

export interface IFormState {
    email: string,
    phoneNumber: string,
    fullName: string,
    message: string;
};