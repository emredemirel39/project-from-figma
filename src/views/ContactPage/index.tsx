import React, { useLayoutEffect } from 'react'

const ContactPage = () => {

  useLayoutEffect(() => {
    window.scrollTo(0, 0);
  })

  return (
    <div>ContactPage</div>
  )
}

export default ContactPage